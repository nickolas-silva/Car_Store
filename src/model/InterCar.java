package model;

public class InterCar extends Car{
    public static int qnt;
    public InterCar(String model, String renavam, Integer year, Double price) {
        super(model, renavam, year, price);
    }
}
