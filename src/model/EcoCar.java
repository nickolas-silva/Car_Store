package model;

public class EcoCar extends Car{
    public static int qnt; 
    public EcoCar(String model, String renavam, Integer year, Double price) {
        super(model, renavam, year, price);
    }
}
